apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: hsts
  namespace: kube-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  headers:
    stsSeconds: 31536000
    stsIncludeSubdomains: true
    stsPreload: false  # 브라우저 프리로드 목록은 제거에 수개월 걸리므로 사용하지 않음
