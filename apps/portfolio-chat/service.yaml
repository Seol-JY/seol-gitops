apiVersion: v1
kind: Service
metadata:
  name: portfolio-chat
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: portfolio-chat
  ports:
    - name: http
      port: 80
      targetPort: http
