title = "seol-gitops"

[extend]
useDefault = true

[allowlist]
description = "SealedSecret encryptedData (RSA-wrapped, base64 always starts with Ag[A-D])"
regexes = [
  '''Ag[A-D][A-Za-z0-9+/=]{200,}''',
]
